<h1>Edycja <%= @ad.name %></h1>

<%= form_for(@ad) do |f| %>
  <% if @ad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ad.errors.count, "error") %> prohibited this ad from being saved:</h2>

      <ul>
      <% @ad.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Nazwa" %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label "Opis" %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label "Cena" %><br />
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label "Link do obrazka" %><br />
    <%= f.text_field :img_url %>
  </div>
  <div class="actions">
    <%= f.submit "Zatwierdź Edycję"%>
  </div>
<% end %>
<%= link_to 'Pokaż', @ad %> |
<%= link_to 'Cofnij', ads_path %> 

